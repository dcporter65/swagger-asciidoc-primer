name: build asciidoc

on:
  push:
    branches:
      - master
jobs:
  asciidoc_build:
    runs-on: ubuntu-18.04
    name: Build Asciidoc
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build HTML
        uses: avattathil/asciidoctor-action@master
        with:
          program: "asciidoctor -D . --backend=html5 ./src/asciidoctor/index.adoc -o ./build/index.html"

      - name: Upload HTML Artifact
        uses: actions/upload-artifact@v1
        with:
          name: HTML document
          path: ./build/index.html

      - name: Build PDF
        uses: avattathil/asciidoctor-action@master
        with:
          program: "asciidoctor-pdf -a allow-uri-read -D . ./src/asciidoctor/index.adoc -o/build/index.pdf"

      - name: Upload PDF Artifact
        uses: actions/upload-artifact@v2
        with:
          name: PDF document
          path: ./build/index.pdf
